description: Intervention information.
type: object
properties:
  id:
    type: integer
    format: int64
  refArticle:
    type: string
  piece:
    $ref: "#/components/schemas/Pieces"
  tasksId:
    type: integer
    format: int64
  task:
    $ref: "#/components/schemas/Tasks"
  quantity:
    type: integer
    format: int64
  createdAt:
    type: string
    format: date
  updatedAt:
    type: string
    format: date
  deleted:
    type: boolean
