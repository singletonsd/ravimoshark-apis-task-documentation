description: Client minimum information.
type: object
properties:
  refClient:
    type: string
  name:
    type: string
  nickname:
    type: string
  group:
    type: string
  addresses:
    type: array
    items:
      $ref: "#/components/schemas/Addresses"
  addressesId:
    type: array
    items:
      type: integer
      format: int64
